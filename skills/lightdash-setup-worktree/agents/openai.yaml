interface:
  display_name: "Setup Lightdash Worktree"
  short_description: "Set up or tear down isolated worktrees"
  default_prompt: "Use $lightdash-setup-worktree to set up or safely tear down an isolated Lightdash development worktree."

policy:
  allow_implicit_invocation: false
